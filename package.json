{
  "name": "turbo-nextjs-php",
  "version": "1.0.0",
  "packageManager": "pnpm@9.1.1",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel -r dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
