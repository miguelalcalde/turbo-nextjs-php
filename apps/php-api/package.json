{
  "name": "php-api",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "dev": "php -S localhost:8080 -t api",
    "start": "php -S localhost:8080 -t api",
    "autoload": "composer dump-autoload",
    "build": "echo 'No build script required for PHP API'"
  }
}
